apiVersion: v1
kind: Service
metadata:
  name: airflow-worker
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: airflow-worker
  ports:
    - name: logs
      port: 8793
      targetPort: 8793
  type: ClusterIP
