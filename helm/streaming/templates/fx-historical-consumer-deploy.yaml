apiVersion: apps/v1
kind: Deployment

metadata:
  name: fx-historical-consumer
  namespace: streaming

spec:
  replicas: 0

  selector:
    matchLabels:
      app: fx-historical-consumer

  template:
    metadata:
      labels:
        app: fx-historical-consumer

    spec:
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}

      serviceAccountName: streaming-sa

      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z kafka 9092; do
                echo "Waiting for Kafka..."
                sleep 5
              done

      containers:
        - name: fx-historical-consumer
          image: "{{ .Values.image.spark.repository }}:{{ .Values.image.spark.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          ports:
            - name: driver-rpc
              containerPort: 7075

            - name: blockmanager
              containerPort: 7082

            - name: ui
              containerPort: 4043

            - name: metrics
              containerPort: 8000

          envFrom:
            - secretRef:
                name: streaming-infra-secret

          env:
            - name: SPARK_LOCAL_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP

            - name: SPARK_DRIVER_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name

          command: ["/bin/bash", "-lc"]

          args:
            - >
              spark-submit \
                --master {{ .Values.spark.k8sMasterUrl }} \
                --deploy-mode client \
                --conf spark.kubernetes.namespace={{ .Values.namespace }} \
                --conf spark.kubernetes.authenticate.driver.serviceAccountName=streaming-sa \
                --conf spark.kubernetes.container.image={{ .Values.image.spark.repository }}:{{ .Values.image.spark.tag }} \
                --conf spark.kubernetes.driver.pod.name=$(SPARK_DRIVER_POD_NAME) \
                --conf spark.driver.host=fx-historical-driver \
                --conf spark.driver.bindAddress=0.0.0.0 \
                --conf spark.driver.port=7075 \
                --conf spark.ui.port=4043 \
                --conf spark.blockManager.port=7082 \
                --conf spark.network.timeout=600s \
                --conf spark.dynamicAllocation.enabled=false \
                --conf spark.pyspark.python=/usr/bin/python3 \
                --conf spark.pyspark.driver.python=/usr/bin/python3 \
                --conf spark.executorEnv.PYSPARK_PYTHON=/usr/bin/python3 \
                --conf spark.executor.instances={{ .Values.spark.executors.fxBackfillReplay }} \
                --conf spark.executor.cores={{ .Values.spark.fxBackfillReplay.executorCores }} \
                --conf spark.executor.memory={{ .Values.spark.fxBackfillReplay.executorMemory }} \
                --conf spark.driver.cores={{ .Values.spark.fxBackfillReplay.driverCores }} \
                --conf spark.driver.memory={{ .Values.spark.fxBackfillReplay.driverMemory }} \
                --conf spark.kubernetes.node.selector.role={{ .Values.nodeSelector.role }} \
                --conf spark.kubernetes.executor.podNamePrefix=fx-historical-exec \
                --conf spark.local.dir=/tmp/spark \
                /app/fx_consumer.py