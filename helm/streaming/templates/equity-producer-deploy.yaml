apiVersion: apps/v1
kind: Deployment
metadata:
  name: equity-producer
  namespace: streaming
spec:
  replicas: {{ .Values.replicas.equityProducer }}

  selector:
    matchLabels:
      app: equity-producer

  template:
    metadata:
      labels:
        app: equity-producer

    spec:
      nodeSelector:
        {{- toYaml .Values.nodeSelector | nindent 8 }}

      serviceAccountName: streaming-sa

      initContainers:
        - name: wait-for-kafka
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z kafka 9092; do
                echo "Waiting for Kafka..."
                sleep 5
              done

      containers:
        - name: equity-producer
          image: "{{ .Values.image.producer.repository }}:{{ .Values.image.producer.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}

          command: ["python"]
          args: ["equity_producer.py"]

          envFrom:
            - secretRef:
                name: streaming-infra-secret

          env:
            - name: RUN_MODE
              value: "live"

---
apiVersion: v1
kind: Service
metadata:
  name: equity-producer
  namespace: streaming
  labels:
    app: equity-producer

spec:
  selector:
    app: equity-producer

  ports:
    - name: metrics
      port: 8000
      targetPort: 8000

---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: equity-producer
  namespace: monitoring
  labels:
    release: monitoring

spec:
  namespaceSelector:
    matchNames:
      - streaming

  selector:
    matchLabels:
      app: equity-producer

  endpoints:
    - port: metrics
      path: /metrics
      interval: 60s