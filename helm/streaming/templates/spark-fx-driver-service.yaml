apiVersion: v1
kind: Service

metadata:
  name: spark-fx-driver
  namespace: {{ .Values.namespace }}

spec:
  clusterIP: None

  selector:
    app: spark-fx

  ports:
    - name: driver-rpc
      port: 7077
      targetPort: 7077