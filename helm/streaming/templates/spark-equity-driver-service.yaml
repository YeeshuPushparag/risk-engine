apiVersion: v1
kind: Service

metadata:
  name: spark-equity-driver
  namespace: {{ .Values.namespace }}

spec:
  clusterIP: None

  selector:
    app: spark-equity

  ports:
    - name: driver-rpc
      port: 7078
      targetPort: 7078