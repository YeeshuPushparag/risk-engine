apiVersion: batch/v1
kind: CronJob
metadata:
  name: stop-fx-producer
  namespace: streaming
spec:
  schedule: "3 17 * * 5"
  timeZone: "America/New_York"
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            {{- toYaml .Values.nodeSelector | nindent 12 }}
          restartPolicy: Never
          serviceAccountName: streaming-sa
          containers:
            - name: stop-fx-producer
              image: bitnami/kubectl:1.30.0
              command:
                - /bin/sh
                - -c
                - kubectl -n streaming scale deployment fx-producer --replicas=0
