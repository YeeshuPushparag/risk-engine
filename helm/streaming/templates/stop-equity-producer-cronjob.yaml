apiVersion: batch/v1
kind: CronJob
metadata:
  name: stop-equity-producer
  namespace: streaming
spec:
  schedule: "3 16 * * 1-5"
  timeZone: "America/New_York"
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            {{- toYaml .Values.nodeSelector | nindent 12 }}
          restartPolicy: Never
          serviceAccountName: streaming-sa
          containers:
            - name: stop-equity-producer
              image: bitnami/kubectl:1.30.0
              command:
                - /bin/sh
                - -c
                - kubectl -n streaming scale deployment equity-producer --replicas=0
